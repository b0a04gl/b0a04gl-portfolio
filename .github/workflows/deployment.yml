name: Deploy Website

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    name: Deploying to surge
    
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-node@v3
        with:
          node-version: 21
      - run: npm install -g surge
      - run: surge ./ https://b0a04gl.surge.sh/ --token bdddc209cc9a9f3ff54826d8e0625c57